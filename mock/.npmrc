# Force public npm registry (default Bosch laptop ones require auth & block install)
# strict-ssl=false is acceptable here because this folder ONLY installs json-server
# for local mock dev — no production code or secrets pulled.
registry=https://registry.npmjs.org/
strict-ssl=false
